<?php

  // PHP menyediakan sejumlah besar variabel yang telah ditentukan sebelumnya ke skrip apapun yang dijalankannya.
  // Namun banyak dari variabel ini tidak dapat didokumentasikan sepenuhnya karena bergantung pada server mana yang dijalankan, versi, dan pengaturan server, maupun faktor lainnya.
  // Variabel ini tergolong ke variabel superglobel
  /*
    - $GLOBALS
    - $_SERVER
    - $_GET
    - $_POST
    - $_FILES
    - $_COOKIE
    - $_SESSION
    - $_REQUEST
    - $_ENV
  */

  var_dump($_SERVER);